











老黄:

PTBST:GwVirtualConfig.properties
Panta FX->Panta RateManager->PantawhiteLabel App->DMM(本番(不可注文),虚拟(可注文))的gw ptbst App->DMM RateManager

EMS(原来叫PANTA):GwVirtualConfig.properties.VR (原来备份的),正常是GwVirtualConfig.properties
Panta FX->Panta RateAgent(CustomerRateTopic)->Panta gw feed app(FxSpotInfo转化成CpSpotRateInfo)--->DMM(本番(不可注文),虚拟(可注文))的gw panta app->DMM RateManager

PANTA和PTBST 这2个从Panta系统引入的汇率,在EM和DMM(本番和虚拟)系统上,都使用,本次只是DMM系统要求把Panta的CPID改成EMS,EM没说改.

EMFX:PANTA,PTBST(新开发的,08.01 UP)
Dmm:PANTA,PTBST(新开发的,07.04 UP)













